# MSF Field Sales Application - Environment Configuration
# This file documents all required environment variables for the application
# Copy this to .env and fill in your values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL Database Connection String
# Format: postgresql://username:password@host:port/database?sslmode=require
# 
# For Replit PostgreSQL:
# This is automatically populated when you provision a database in Replit
#
# For GCP Cloud SQL:
# Example: postgresql://postgres:YOUR_PASSWORD@/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME/DATABASE_NAME?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME
# Or with external IP: postgresql://postgres:YOUR_PASSWORD@EXTERNAL_IP:5432/DATABASE_NAME?sslmode=require
#
DATABASE_URL=postgresql://user:password@host:port/database

# =============================================================================
# HUBSPOT INTEGRATION
# =============================================================================

# HubSpot Private App API Key
# Get this from: HubSpot → Settings → Integrations → Private Apps
# Required scopes:
#   - crm.objects.companies.read
#   - crm.objects.custom.read
#   - crm.objects.custom.write
#   - crm.objects.owners.read (optional)
#
HUBSPOT_API_KEY=pat-na1-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# HubSpot Field Visit Association Type ID (Optional)
# Only needed if you want automatic company association for field visits
# See HUBSPOT_CUSTOM_OBJECT_SETUP.md for how to get this value
#
# HUBSPOT_FIELD_VISIT_ASSOCIATION_TYPE_ID=123

# =============================================================================
# MAPBOX CONFIGURATION
# =============================================================================

# Mapbox Access Token (Server-side)
# Get from: https://account.mapbox.com/
# Used for geocoding and route optimization APIs
# Free tier: 100,000 geocoding requests/month
#
MAPBOX_TOKEN=pk.eyJ1IjoieW91cnVzZXJuYW1lIiwiYSI6InlvdXJ0b2tlbiJ9.xxxxxxxxxxxxxxxxxxxxx

# Mapbox Access Token (Client-side)
# Must be the SAME value as MAPBOX_TOKEN
# Required for frontend map display
# Note: In Vite, only env vars prefixed with VITE_ are exposed to the client
#
VITE_MAPBOX_TOKEN=pk.eyJ1IjoieW91cnVzZXJuYW1lIiwiYSI6InlvdXJ0b2tlbiJ9.xxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# SESSION CONFIGURATION
# =============================================================================

# Session Secret for encrypting session cookies
# Generate a secure random string with: openssl rand -hex 32
# IMPORTANT: Keep this secret and never commit to version control
#
SESSION_SECRET=your-secure-random-session-secret-here-use-openssl-rand-hex-32

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Node Environment
# Values: development | production
NODE_ENV=development

# Server Port (Optional - defaults to 5000)
# PORT=5000

# =============================================================================
# GCP DEPLOYMENT NOTES
# =============================================================================
#
# When deploying to GCP, you have several options for managing secrets:
#
# 1. Google Cloud Secret Manager (Recommended for Production)
#    - Store secrets in Secret Manager
#    - Access them in your app using @google-cloud/secret-manager
#    - Most secure option for production deployments
#
# 2. Environment Variables in Cloud Run / App Engine
#    - Set environment variables in your deployment configuration
#    - Example (Cloud Run): gcloud run deploy --set-env-vars DATABASE_URL=xxx,HUBSPOT_API_KEY=yyy
#    - Good for non-production environments
#
# 3. .env file (Development Only)
#    - DO NOT deploy .env files to production
#    - Add .env to .gitignore (already done)
#    - Only for local development
#
# For Cloud SQL on GCP:
# - Use Cloud SQL Proxy for secure connections
# - Connection string format: postgresql://USER:PASSWORD@/DATABASE?host=/cloudsql/PROJECT:REGION:INSTANCE
# - Enable Cloud SQL Admin API in your GCP project
#
# =============================================================================
